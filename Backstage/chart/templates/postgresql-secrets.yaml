# postgresql secrets
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "backstage.fullname" . }}-postgres-secrets
type: Opaque
data:
  POSTGRES_USER: {{ .Values.pg_user | b64enc }}
  POSTGRES_PASSWORD: {{ .Values.pg_pass | b64enc }}